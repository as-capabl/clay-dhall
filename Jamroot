
local dist-dir = [ SHELL "stack path --dist-dir" : strip-eol ] ;
local local-install-root = [ SHELL "stack path --local-install-root" : strip-eol ] ;

lib claydhall_test_link
    :
    : <name>clay-dhall <search>$(dist-dir)/build/clay-dhall
    :
    : <include>include <library-path>.$(local-install-root)/lib
    ;

